name: my_app
description: A Flutter project untuk UTS Keranjang Dessert
publish_to: 'none' # ini supaya nggak publish ke pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  # Tambahan opsional (sesuai kebutuhan)
  cupertino_icons: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # Folder assets
  assets:
    - assets/images/
